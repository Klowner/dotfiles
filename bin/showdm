#!/bin/bash
export WINDIR=1

case "$HOSTNAME" in
	"finn")
		$(dmenu_path_c | dmenu)
	;;
	*)
		if [[ -e /usr/bin/yeganesh ]]; then
			$(dmenu_path_c | yeganesh)
		else
			$(dmenu_path_c | dmenu)
		fi
esac
