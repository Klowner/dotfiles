# vim: set ft=zsh:
if [[ -z $commands[eza] ]]; then
	return 0;
fi

alias ls='eza -g'                 # ls
alias l='eza -lbgF --git'       # List, size, type git
alias ll='eza -lbgGF --git'     # Long list
alias llm='eza -lbgGF --git --sort=modified'     # Long list

